# 🛠️ Папка `utils/` — Утилиты и вспомогательные функции

Эта папка содержит **маленькие полезные инструменты**, которые используются в разных частях программы. Как ящик с отвёртками и молотком — каждый инструмент делает свою конкретную задачу.

---

## 🤔 Что такое "утилиты"?

**Утилита** — это небольшая функция или набор функций, которые:
- Выполняют одну конкретную задачу
- Не зависят от остальной программы
- Могут использоваться в разных местах

**Примеры утилит:**
- Генератор случайных чисел
- Функция форматирования даты
- Конвертер валют

---

## 📄 Файл `password-generator.ts`

Этот файл отвечает за создание случайных паролей. Он содержит функции, которые генерируют надёжные пароли.

---

## 🔍 Разбор кода

### Константы настроек

```typescript
const DEFAULT_OPTIONS: Required<PasswordOptions> = {
  length: 16,              // Длина по умолчанию — 16 символов
  includeUppercase: true,  // Использовать заглавные буквы
  includeLowercase: true,  // Использовать строчные буквы
  includeNumbers: true,    // Использовать цифры
  includeSymbols: true     // Использовать спецсимволы
};

const CHAR_SETS = {
  uppercase: 'ABCDEFGHIJKLMNOPQRSTUVWXYZ',  // 26 букв
  lowercase: 'abcdefghijklmnopqrstuvwxyz',  // 26 букв
  numbers: '0123456789',                      // 10 цифр
  symbols: '!@#$%^&*'                         // 8 символов
};
```

**Что такое `Required<PasswordOptions>`:**

Это TypeScript-тип, который делает все поля обязательными (убирает `?`). Мы точно знаем, что здесь будут все поля.

---

### Криптографически безопасный генератор

```typescript
function getSecureRandomInt(max: number): number {
  // Получаем 4 случайных байта (32 бита)
  const randomBytes = getRandomValues(new Uint32Array(1));
  const value = randomBytes[0];
  if (value === undefined) {
    throw new Error('Failed to generate secure random value');
  }
  // Берём остаток от деления — получаем число от 0 до max-1
  return value % max;
}
```

**Почему это важно:**

Обычный `Math.random()` — это **псевдослучайный** генератор. Он создаёт числа по формуле, и если знать формулу — можно предсказать следующее число.

`getRandomValues` использует **настоящую случайность** от операционной системы. Такие числа невозможно предсказать — это критично для паролей!

**Как работает:**

1. `new Uint32Array(1)` — создаём массив из одного 32-битного числа
2. `getRandomValues(...)` — заполняем его случайными байтами
3. `value % max` — превращаем в число от 0 до max-1

**Пример:**
```
Допустим, max = 10 (нам нужны цифры 0-9)

Случайное число: 2_147_483_647 (максимум для Uint32)
2_147_483_647 % 10 = 7

Случайное число: 1_234_567_890
1_234_567_890 % 10 = 0

Случайное число: 987_654_321
987_654_321 % 10 = 1
```

---

### Основная функция генерации

```typescript
export function generatePassword(options: Partial<PasswordOptions> = {}): string {
  // 1. Объединяем переданные опции с настройками по умолчанию
  const config = { ...DEFAULT_OPTIONS, ...options };
  
  // 2. Собираем строку доступных символов
  let chars = '';
  if (config.includeUppercase) chars += CHAR_SETS.uppercase;
  if (config.includeLowercase) chars += CHAR_SETS.lowercase;
  if (config.includeNumbers) chars += CHAR_SETS.numbers;
  if (config.includeSymbols) chars += CHAR_SETS.symbols;
  
  // 3. Проверяем, что есть хоть какие-то символы
  if (chars.length === 0) {
    throw new Error('At least one character set must be selected');
  }
  
  // 4. Генерируем пароль
  let password = '';
  for (let i = 0; i < config.length; i++) {
    // Случайный индекс от 0 до длины строки символов
    const randomIndex = getSecureRandomInt(chars.length);
    // Берём символ по этому индексу
    password += chars.charAt(randomIndex);
  }
  
  // 5. Возвращаем результат
  return password;
}
```

**Пошаговый пример:**

```typescript
// Вызываем generatePassword({ length: 8, includeSymbols: false })

// Шаг 1: config = { length: 8, includeUppercase: true, ... }
// Шаг 2: chars = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789"
//         (62 символа, без спецсимволов)

// Шаг 4: Цикл 8 раз
// Итерация 1: randomIndex = 15 → chars[15] = 'P' → password = "P"
// Итерация 2: randomIndex = 38 → chars[38] = 'm' → password = "Pm"
// Итерация 3: randomIndex = 7  → chars[7]  = 'H' → password = "PmH"
// ... и так далее

// Результат: "PmH7kL2n"
```

---

### Упрощённая функция

```typescript
export function generateSecurePassword(length: number = 16): string {
  return generatePassword({
    length,
    includeUppercase: true,
    includeLowercase: true,
    includeNumbers: true,
    includeSymbols: true
  });
}
```

Эта функция — **обёртка** над основной. Она всегда использует все типы символов, только длину можно менять.

**Почему нужна:**

Чтобы не писать каждый раз:
```typescript
// Вместо этого:
generatePassword({
  length: 16,
  includeUppercase: true,
  includeLowercase: true,
  includeNumbers: true,
  includeSymbols: true
})

// Можно просто:
generateSecurePassword(16)
```

---

## 🎯 Примеры использования

### Простой пароль (по умолчанию)

```typescript
import { generateSecurePassword } from './utils/password-generator.ts';

const password = generateSecurePassword();
console.log(password);  // "K9#mP$vL2@nQ5*wR"
// 16 символов, все типы символов
```

### Пароль определённой длины

```typescript
const shortPassword = generateSecurePassword(8);
console.log(shortPassword);  // "Ab3$dFg7"

const longPassword = generateSecurePassword(32);
console.log(longPassword);   // "K9#mP$vL2@nQ5*wR8!xZ3$jH5^kL7@mN"
```

### Пароль с кастомными настройками

```typescript
import { generatePassword } from './utils/password-generator.ts';

// Только буквы и цифры (без спецсимволов)
const easyPassword = generatePassword({
  length: 12,
  includeSymbols: false
});
console.log(easyPassword);  // "AbC3dEfG7hJk"

// Только цифры (PIN-код)
const pinCode = generatePassword({
  length: 4,
  includeUppercase: false,
  includeLowercase: false,
  includeSymbols: false,
  includeNumbers: true
});
console.log(pinCode);  // "7392"

// Сложный пароль с большой длиной
const veryStrong = generatePassword({
  length: 64,
  includeUppercase: true,
  includeLowercase: true,
  includeNumbers: true,
  includeSymbols: true
});
```

---

## 📊 Математика паролей

### Сколько возможных паролей?

Количество вариантов = (количество символов) ^ (длина пароля)

**Пример 1 — Простой пароль (только строчные буквы, 6 символов):**
```
26^6 = 308_915_776 вариантов
~ 309 миллионов
```

**Пример 2 — Наш стандарт (все символы, 16 символов):**
```
(26 + 26 + 10 + 8)^16 = 70^16
= 3_323_293_056_762_334_451_542_074_089_062_500

~ 3.3 × 10^29
```

**Насколько это много?**

Если попробовать подобрать пароль перебором:
- 1 миллиард попыток в секунду
- Понадобится ~ 10^13 лет
- Возраст Вселенной ~ 13.8 × 10^9 лет

**Вывод:** Пароль из 16 случайных символов невозможно взломать перебором.

---

## 🔐 Почему это безопасно?

### 1. Криптографическая случайность

```typescript
import { getRandomValues } from 'node:crypto';
```

`node:crypto` — это модуль Node.js, который использует криптографические функции операционной системы. Эти числа:
- Не предсказуемы
- Не повторяются
- Проходят статистические тесты случайности

### 2. Равномерное распределение

Оператор `%` (остаток от деления) даёт равномерное распределение:

```
Случайное число: 0-4_294_967_295 (максимум Uint32)
max: 62 (количество символов)

Результат: 0-61 с равной вероятностью
```

**Важно:** Это не идеально равномерное (из-за остатка), но для практических целей достаточно.

### 3. Нет закономерностей

В отличие от человека, генератор:
- Не использует словарные слова
- Не добавляет даты или имена
- Не повторяет паттерны

---

## 💡 Сравнение с человеческими паролями

| Пароль | Как создал | Сложность |
|--------|------------|-----------|
| `password123` | Человек придумал | ❌ Очень слабый |
| `Ivan1990` | Имя + год рождения | ❌ Легко угадать |
| `Tr0ub4dor&3` | Человек "усложнил" | ⚠️ Средний |
| `K9#mP$vL2@nQ5*wR` | Наш генератор | ✅ Очень сильный |

**Почему `Tr0ub4dor&3` не так хорош:**
- Слово "troubadour" с заменами
- Закономерность: слово + цифра + символ
- Можно взломать словарной атакой

**Почему `K9#mP$vL2@nQ5*wR` отличный:**
- Никаких закономерностей
- Полностью случайный
- Большое количество вариантов

---

## 🔧 Расширение функциональности

### Идеи для улучшения:

```typescript
// Генерация запоминающегося пароля
export function generateMemorablePassword(wordCount: number = 4): string {
  const words = ['correct', 'horse', 'battery', 'staple', ...];
  // correct-horse-battery-staple
}

// Проверка сложности пароля
export function checkPasswordStrength(password: string): 'weak' | 'medium' | 'strong' {
  // Проверка длины, наличия разных типов символов
}

// Генерация с исключением похожих символов
export function generatePasswordWithoutConfusables(length: number): string {
  // Исключаем: 0 и O, 1 и l и I
}
```

---

## 📝 Итог

| Аспект | Описание |
|--------|----------|
| **Безопасность** | Используется криптографически стойкий генератор |
| **Гибкость** | Можно настроить длину и типы символов |
| **Простота** | Два способа вызова — полный и упрощённый |
| **Надёжность** | Пароль из 16 символов невозможно взломать перебором |

---

**Это завершает обзор всех частей программы! Теперь вы понимаете, как работает каждый модуль. Рекомендуем вернуться к главному README и попробовать запустить программу.**
