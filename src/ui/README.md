# 🖥️ Папка `ui/` — Интерфейс пользователя

Эта папка содержит всё, что связано с **отображением** программы — меню, формы, таблицы и обработку нажатий клавиш.

---

## 🤔 Что такое "UI"?

**UI (User Interface)** — это "лицо" программы, то, что видит и с чем взаимодействует пользователь.

Если программа — это ресторан, то UI — это:
- Меню (список блюд)
- Столики (где сидеть)
- Официант (принимает заказ)
- Кухня — это **не UI**, это бизнес-логика

Наша программа работает в терминале (командной строке), но у неё всё равно есть красивый интерфейс с меню, формами и цветами!

---

## 📄 Файлы в этой папке

| Файл | Назначение |
|------|------------|
| `components.ts` | Строительные блоки интерфейса (кнопки, поля, цвета) |
| `password-manager-ui.ts` | Главный класс UI, собирает всё воедино |

---

## 🎨 `components.ts` — Компоненты интерфейса

Этот файл содержит **фабрику** — набор функций, которые создают элементы интерфейса. Это как набор инструментов для строителя.

### Цветовая схема (COLORS)

```typescript
export const COLORS = {
  primary: 'blue',          // Основной цвет
  primaryBright: 'bright-blue',
  secondary: 'cyan',        // Вторичный цвет
  accent: 'green',          // Акцентный цвет (успех)
  accentBright: 'bright-green',
  warning: 'yellow',        // Предупреждения
  error: 'red',             // Ошибки
  text: 'white',            // Текст
  textMuted: 'gray',        // Приглушённый текст
  bg: 'black',              // Фон
  border: 'blue',           // Границы
  borderFocus: 'bright-cyan', // Активная граница
  highlight: 'bright-yellow'  // Подсветка
};
```

**Зачем это нужно:**

Вместо того чтобы писать цвета в каждом месте (`'blue'`, `'red'`), мы используем константы (`COLORS.primary`, `COLORS.error`). Если захотим изменить цвета — поменяем в одном месте!

---

### Создание экрана

```typescript
export function createScreen(title: string): blessed.Widgets.Screen {
  return blessed.screen({
    smartCSR: true,           // Умная перерисовка экрана
    title: title,             // Заголовок окна терминала
    terminal: 'xterm-256color' // Поддержка 256 цветов
  });
}
```

**Что такое Screen:**

Это "холст" — основа, на которую мы будем размещать все элементы. Представьте пустой лист бумаги.

---

### Создание заголовка

```typescript
export function createTitle(content: string): blessed.Widgets.BoxElement {
  return blessed.box({
    top: 1,                   // Отступ сверху (1 строка)
    left: 'center',           // Выравнивание по центру
    width: 'shrink',          // Ширина по содержимому
    height: 3,                // Высота 3 строки
    content: '{center}{bright-green-fg}{bold}🔐 Менеджер паролей{/bold}{/bright-green-fg}{/center}',
    tags: true,               // Разрешить теги форматирования
    style: { fg: 'bright-green' }
  });
}
```

**Что такое теги форматирования:**

Текст внутри `{...}` — это инструкции для отрисовки:
- `{center}` — выровнять по центру
- `{bright-green-fg}` — ярко-зелёный цвет текста
- `{bold}` — жирный шрифт
- `{/bold}` — закрыть тег (конец жирного)

---

### Создание меню

```typescript
export function createMenu(config: MenuConfig): blessed.Widgets.ListElement {
  return blessed.list({
    top: config.top,
    left: config.left,
    width: config.width,
    height: config.height,
    items: config.items.map((item, i) => `  ${i + 1}. ${item}`),
    label: ' Меню ',
    border: { type: 'line' },
    tags: true,
    style: {
      selected: {             // Стиль выбранного пункта
        bg: 'blue', 
        fg: 'white',
        bold: true
      },
      item: { fg: 'white' },  // Стиль обычных пунктов
      border: { fg: 'blue' }, // Цвет границы
      label: { fg: 'green', bold: true }
    },
    keys: true,               // Разрешить клавиши управления
    vi: true,                 // Режим vi (как в Vim)
    mouse: true               // Поддержка мыши
  });
}
```

**Что такое List:**

Это список элементов, по которому можно перемещаться стрелками. Представьте меню в игре на приставке.

**Режим VI:**

`vi: true` позволяет использовать клавиши из редактора Vim:
- `j` — вниз
- `k` — вверх
- `Enter` — выбрать

---

### Создание поля ввода

```typescript
export function createPrompt(screen, config): blessed.Widgets.TextboxElement {
  return blessed.textbox({
    parent: screen,           // Родительский элемент
    top: config.top,
    left: 'center',
    width: config.width || 50,
    height: 3,
    label: ` {bold}${config.label}{/bold} `,
    border: { type: 'line' },
    tags: true,
    style: {
      border: { fg: 'blue' },
      focus: { border: { fg: 'bright-cyan' } },  // При фокусе
      label: { fg: 'green', bold: true }
    },
    inputOnFocus: true,       // Начать ввод при фокусе
    secret: config.password || false,  // Скрыть ввод (для паролей)
    value: config.value || ''
  });
}
```

**Что такое `secret: true`:**

Когда вы вводите пароль, вместо букв показываются звёздочки: `********`

---

### Создание кнопки

```typescript
export function createButton(options): blessed.Widgets.ButtonElement {
  return blessed.button({
    parent: options.parent,
    content: `{center}{bold}✓ Сохранить{/bold}{/center}`,
    align: 'center',
    valign: 'middle',
    border: { type: 'line' },
    tags: true,
    style: {
      border: { fg: options.fg || 'blue' },
      bg: options.bg || 'default',
      fg: options.fg || 'white',
      focus: { 
        bg: options.bg || 'blue', 
        fg: 'white',
        border: { fg: 'bright-cyan' }
      }
    },
    keys: true,               // Можно выбрать клавишами
    mouse: true               // Можно нажать мышью
  });
}
```

---

### Выход из программы

```typescript
export function setupExitHandler(screen, keys = ['q', 'C-c']): void {
  screen.key(keys, () => {
    process.exit(0);          // Корректный выход
  });
}
```

**Что это делает:**

Когда вы нажимаете `q` или `Ctrl+C`, программа закрывается корректно.

---

## 🎛️ `password-manager-ui.ts` — Главный класс UI

Этот файл содержит класс `PasswordManagerUI` — он собирает все компоненты в рабочий интерфейс.

### Структура класса

```typescript
export class PasswordManagerUI {
  private screen: blessed.Widgets.Screen;      // Основной экран
  private menu: blessed.Widgets.ListElement;   // Главное меню
  private output: blessed.Widgets.BoxElement;  // Область сообщений
  private hint: blessed.Widgets.BoxElement;    // Подсказки
  private onMenuSelect: (index: number) => void; // Обработчик меню
  private activeFormElements: blessed.Widgets.BlessedElement[] = [];
  private passwordListItems: PasswordListItem[] = [];
  private passwordListElement: blessed.Widgets.ListElement | null = null;
  private passwordDetailBox: blessed.Widgets.BoxElement | null = null;
}
```

### Конструктор (создание интерфейса)

```typescript
constructor(onMenuSelect: (index: number) => void) {
  // Сохраняем обработчик выбора меню
  this.onMenuSelect = onMenuSelect;

  // 1. Создаём экран
  this.screen = createScreen('🔐 Менеджер паролей');

  // 2. Создаём заголовок
  const title = createTitle('🔐 Менеджер паролей');

  // 3. Создаём главный контейнер
  const mainBox = createBox({...});

  // 4. Создаём меню с пунктами
  this.menu = createMenu({
    items: [
      'Посмотреть пароли',
      'Добавить пароль',
      'Сгенерировать пароль',
      'Выйти'
    ]
  });

  // 5. Создаём область для сообщений
  this.output = createOutputBox('Добро пожаловать!');

  // 6. Создаём подсказку с клавишами
  this.hint = createHint('↑↓ / Ctrl+N / Ctrl+P — выбор | Enter — подтвердить | q — выход');

  // 7. Добавляем всё на экран
  this.screen.append(title);
  this.screen.append(this.menu);
  this.screen.append(this.output);
  this.screen.append(this.hint);

  // 8. Устанавливаем фокус на меню
  this.menu.focus();

  // 9. Настраиваем обработчики
  this.setupEventHandlers();
  this.setupNavigationKeys();
  setupExitHandler(this.screen);
}
```

**Порядок создания важен!**

Элементы добавляются на экран слоями. То, что добавлено позже, рисуется поверх предыдущего.

---

### Показ формы ввода

```typescript
public async showForm(
  title: string,
  fields: FormField[],
  onGeneratePassword?: () => string
): Promise<FormResult | null> {
  // 1. Скрываем меню
  this.menu.hide();
  this.output.hide();
  this.hint.hide();

  // 2. Создаём контейнер для формы
  const box = blessed.box({
    parent: this.screen,
    top: 'center',          // По центру экрана
    left: 'center',
    width: 80,
    height: fields.length * 5 + 12,
    border: { type: 'line' },
    label: ` ${title} `
  });

  // 3. Создаём поля ввода
  const inputs: blessed.Widgets.TextboxElement[] = [];
  for (let i = 0; i < fields.length; i++) {
    const field = fields[i]!;

    // Подпись поля
    blessed.text({
      parent: box,
      top: i * 5 + 2,
      content: `{bold}${field.label}:{/bold}`
    });

    // Поле ввода
    const input = blessed.textbox({
      parent: box,
      top: i * 5 + 3,
      secret: field.password ?? false,  // Скрывать если пароль
      value: field.value ?? ''
    });

    inputs.push(input);
  }

  // 4. Создаём кнопки
  const submitBtn = createButton({ content: '✓ Сохранить', ... });
  const cancelBtn = createButton({ content: '✕ Отмена', ... });

  // 5. Возвращаем Promise — ждём действия пользователя
  return new Promise((resolve) => {
    // Обработка сохранения
    const submit = () => {
      const result: FormResult = {};
      for (let i = 0; i < fields.length; i++) {
        const fieldName = fields[i]!.name;
        const input = inputs[i];
        const value = input ? input.getValue() || '' : '';
        result[fieldName] = value;
      }
      // Восстанавливаем интерфейс
      this.clearForm();
      this.menu.show();
      this.output.show();
      this.hint.show();
      this.menu.focus();
      this.screen.render();
      resolve(result);  // Возвращаем данные
    };

    // Обработка отмены
    const cancel = () => {
      this.clearForm();
      this.menu.show();
      this.output.show();
      this.hint.show();
      this.menu.focus();
      this.screen.render();
      resolve(null);  // Возвращаем null
    };

    // Назначаем обработчики
    submitBtn.on('press', submit);
    cancelBtn.on('press', cancel);
    this.screen.key(['escape'], cancel);

    // Навигация по Tab
    this.screen.key(['tab'], () => {
      // Переключаем фокус между полями
    });
  });
}
```

**Что такое Promise:**

Форма не знает, когда пользователь нажмёт кнопку. Поэтому она возвращает **обещание** (`Promise`), которое "выполнится" позже — когда пользователь что-то сделает.

**Async/Await:**

```typescript
// Вызывающий код ждёт результат:
const result = await ui.showForm('Добавить пароль', fields);
if (result) {
  // Пользователь нажал "Сохранить"
  console.log(result.service);  // "google.com"
} else {
  // Пользователь нажал "Отмена"
}
```

---

### Показ списка паролей

```typescript
public async showPasswordList(
  passwords: PasswordEntry[],
  onBack: () => void
): Promise<void> {
  // 1. Скрываем меню
  this.clearForm();
  this.menu.hide();
  this.output.hide();
  this.hint.hide();

  // 2. Создаём контейнер
  const container = blessed.box({...});

  // 3. Заголовок таблицы
  const headerBox = blessed.box({
    content: `{bold}{green-fg}  №  │  Сервис                │  Логин                 │  Пароль{/green-fg}{/bold}`
  });

  // 4. Список паролей
  this.passwordListElement = blessed.list({
    style: {
      selected: { bg: 'blue', fg: 'white', bold: true }
    },
    keys: true,
    vi: true,
    mouse: true
  });

  // 5. Блок с деталями
  this.passwordDetailBox = blessed.box({
    border: { type: 'line' },
    label: ' {bold}Детали записи{/bold} '
  });

  // 6. Обновляем список
  this.updatePasswordList();

  // 7. Обработчик выбора
  this.passwordListElement.on('select', (_item, index) => {
    const item = this.passwordListItems[index];
    if (item) {
      // Показываем/скрываем пароль
      item.revealed = !item.revealed;
      this.updatePasswordList();
      this.updatePasswordDetail(index);
      // Копируем в буфер обмена
      if (item.revealed) {
        clipboardy.writeSync(item.entry.password);
      }
    }
  });

  // 8. Обработчик возврата
  this.screen.key(['escape'], () => {
    this.clearForm();
    this.menu.show();
    this.output.show();
    this.hint.show();
    this.menu.focus();
    this.screen.render();
    onBack();
  });
}
```

**Как это выглядит:**

```
┌─────────────────────────────────────────────────────────────────────────┐
│ 🔐 Сохранённые пароли                                                   │
├─────────────────────────────────────────────────────────────────────────┤
│  №  │  Сервис                │  Логин                 │  Пароль         │
├─────┼────────────────────────┼────────────────────────┼─────────────────┤
│▶ 1  │  google.com            │  ivan@gmail.com        │  ••••••••••     │
│  2  │  vk.com                │  ivan_1990             │  ••••••••••     │
│  3  │  github.com            │  developer_ivan        │  ••••••••••     │
└─────────────────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────────────────┐
│ Детали записи                                                           │
│                                                                         │
│   Сервис:  google.com                                                   │
│   Логин:   ivan@gmail.com                                               │
│   Пароль:  •••••••••• (нажмите Enter чтобы показать)                    │
└─────────────────────────────────────────────────────────────────────────┘
```

---

### Маскировка пароля

```typescript
private updatePasswordList(): void {
  const items = this.passwordListItems.map((item, index) => {
    const num = String(index + 1).padStart(2, ' ');
    const service = item.entry.service.slice(0, 20).padEnd(20, ' ');
    const username = item.entry.username.slice(0, 20).padEnd(20, ' ');
    
    // Если пароль раскрыт — показываем, иначе точки
    const maskedPassword = item.revealed
      ? item.entry.password.slice(0, 25)
      : '•'.repeat(Math.min(item.entry.password.length, 20));
    
    return `  ${num} │  ${service}  │  ${username}  │  ${maskedPassword}`;
  });

  this.passwordListElement.setItems(items);
  this.screen.render();
}
```

**Функции форматирования:**

- `padStart(2, ' ')` — дополнить слева пробелами до длины 2
- `padEnd(20, ' ')` — дополнить справа пробелами до длины 20
- `slice(0, 20)` — взять первые 20 символов

---

### Ввод мастер-пароля

```typescript
public async askPassword(prompt: string): Promise<string | null> {
  // Скрываем всё
  this.clearForm();
  this.menu.hide();
  this.output.hide();
  this.hint.hide();

  // Создаём окно аутентификации
  const box = blessed.box({
    top: 'center',
    left: 'center',
    width: 60,
    height: 12,
    border: { type: 'line' },
    label: ' 🔐 Аутентификация '
  });

  // Текст подсказки
  blessed.text({
    parent: box,
    content: `{bold}${prompt}{/bold}`
  });

  // Поле для пароля (secret: true)
  const input = blessed.textbox({
    parent: box,
    secret: true,  // Скрываем ввод!
    inputOnFocus: true
  });

  input.focus();
  this.screen.render();

  // Ждём ввода
  return new Promise((resolve) => {
    input.on('submit', (value: string) => {
      // Восстанавливаем интерфейс
      this.clearForm();
      this.menu.show();
      this.output.show();
      this.hint.show();
      this.menu.focus();
      this.screen.render();
      resolve(value.trim() || null);
    });

    // Можно отменить по Escape
    this.screen.key(['escape'], () => {
      ...
      resolve(null);
    });
  });
}
```

---

## 🔄 Жизненный цикл UI

```
┌─────────────────────────────────────────────────────────────┐
│  1. СОЗДАНИЕ (constructor)                                  │
│  • Создаётся экран                                          │
│  • Добавляются компоненты (меню, заголовок, подсказки)      │
│  • Назначаются обработчики событий                          │
│  • Показывается главное меню                                │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│  2. ОЖИДАНИЕ ВВОДА                                          │
│  Пользователь выбирает пункт меню → срабатывает onMenuSelect│
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│  3. ПОКАЗ ФОРМЫ/СПИСКА (showForm / showPasswordList)        │
│  • Скрывается меню                                          │
│  • Создаётся форма или список                               │
│  • Ждём ввода пользователя (Promise)                        │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│  4. ВОЗВРАТ К МЕНЮ                                          │
│  • Уничтожаем форму (clearForm)                             │
│  • Показываем меню снова                                    │
│  • Устанавливаем фокус                                      │
└─────────────────────────────────────────────────────────────┘
```

---

## 💡 Ключевые концепции

### Рендеринг (отрисовка)

```typescript
this.screen.render();
```

Эта команда говорит blessed: "Нарисуй всё, что мы настроили, на экране". Без неё изменения не появятся!

### Фокус

```typescript
this.menu.focus();
```

Только один элемент может быть "активным" в данный момент. Клавиши управления работают для элемента в фокусе.

### События (Events)

```typescript
this.menu.on('select', (_item, index) => {
  this.onMenuSelect(index);
});
```

Blessed использует событийную модель. Когда что-то происходит (выбор пункта, нажатие клавиши), вызывается функция-обработчик.

### Горячие клавиши

```typescript
this.screen.key(['C-g'], () => {
  generate();  // Ctrl+G
  return false;
});
```

`'C-g'` означает Ctrl+G. Можно назначить несколько клавиш: `['q', 'C-c']` — q или Ctrl+C.

---

## 📚 Полезные ссылки

- [Документация Blessed](https://github.com/chjj/blessed)
- [Blessed Tutorial](https://github.com/chjj/blessed/blob/master/example/simple-form.js)

---

**Теперь вы понимаете, как работает интерфейс! Переходите к изучению утилит, чтобы узнать про генерацию паролей.**
